// install gd
// update gd
// install script14
// install python
// set python PATH://
// set win11 layout

pip install discord-webhook
pip install pyTelegramBotAPI

// set the gd layout
// testing script.

// close the client version

// install google remote
// copy the least source codes to workspace.

// copy the vm pc (1-5)

// login the google remote.
